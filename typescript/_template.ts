import { solvePart } from "./_utils.ts";

const year = 2024;
const day = 0;
const example = ``;

solvePart(year, day, 1, part1, { input: example, result: -1 });
solvePart(year, day, 2, part2, { input: example, result: -1 });

function part1(input: string) {
  const {} = parseInput(input);

  return 0;
}

function part2(input: string) {
  const {} = parseInput(input);

  return 0;
}

function parseInput(input: string) {
  return {};
}
